{
  "name": "random-number-mcp-server",
  "version": "1.0.0", 
  "description": "MCP server for random number generation deployed on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

